spring:
  security:
    oauth2:
      client:
        registration:
          azure:
            client-id: "ENC(keepass-user:Vokabel-App-Registration)"
            client-secret: "ENC(keepass:Vokabel-App-Registration)"
            scope: openid,profile,email
        provider:
          azure:
            issuer-uri: https://login.microsoftonline.com/f5adcf45-2c85-41a0-aa4c-5a92714cc069/v2.0
  datasource:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    url: jdbc:hsqldb:file:target\vokabel-app-db
    username: "ENC(keepass-user:Vokabel-App-Datenbank)"
    password: "ENC(keepass:Vokabel-App-Datenbank)"
  jpa:
    database:
    database-platform: org.hibernate.dialect.HSQLDialect
