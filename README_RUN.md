Run the Spring Boot server locally:

Requirements: Java 17+, Maven

Commands:

# from /sourcecode/v2
mvn -q package
java -jar target/vokabel-server-0.0.1-SNAPSHOT.jar

Then open http://localhost:8080 in your phone (use PC IP if accessing from another device).